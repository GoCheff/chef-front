import tw from "twin.macro";

const Container = tw.div`flex items-center justify-center h-screen w-screen`;

const Wrapper = tw.div`flex flex-col items-center justify-center bg-white px-6 py-10 w-full sm:max-w-sm max-w-md shadow rounded`;

const S = { Container, Wrapper };

export { S };
